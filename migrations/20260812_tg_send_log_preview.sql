-- Что именно бот пытался отправить.
--
-- В журнале отправок были только метод и текст ошибки: при жалобе «бот
-- прислал ерунду» или «ничего не пришло» посмотреть было нечего. Храним
-- первые 200 символов сообщения — этого хватает, чтобы узнать экран.

ALTER TABLE tg_send_log
  ADD COLUMN text_preview VARCHAR(200) NULL DEFAULT NULL AFTER error_text;
